[build]
command = "npm run build"
publish = "dist/portfolioProject/browser"

# Optional: redirect all routes to index.html for Angular routing
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
